import { atom } from 'jotai';

// the logo can be activated on pages where a single specific agency is active, like a chat session
// currently logo is used in the header
export const agencyLogoAtom = atom('');
