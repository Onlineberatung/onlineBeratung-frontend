import * as React from 'react';

interface RegistrationAgeProps {
	onAgeChange?: Function;
	onValidityChange?: Function;
}

export const RegistrationAge = (props: RegistrationAgeProps) => {
	return <div>age select</div>;
};
