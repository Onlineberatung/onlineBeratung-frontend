// we use a seperate file so cypress is able to import. importing full
// components would require to config cypress with the CRA webpack config (also
// required for component testing)
export const MAX_ITEMS_TO_SHOW_WELCOME_ILLUSTRATION = 3;
export const SCROLL_PAGINATE_THRESHOLD = 50; // pixel
