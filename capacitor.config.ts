import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Caritas-OnlineBeratung.app',
  appName: 'Caritas Online-Beratung',
  webDir: './src/pages',
  bundledWebRuntime: false
};

export default config;
